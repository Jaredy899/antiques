export const metadata = {
  title: "Image Gallery | Upload and Share Images",
  description: "Upload and share your images in our simple gallery.",
};

export default function AntiquesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
} 